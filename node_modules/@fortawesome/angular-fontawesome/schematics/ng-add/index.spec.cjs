"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const schematics_1 = require("@angular-devkit/schematics");
const testing_1 = require("@angular-devkit/schematics/testing");
const path = __importStar(require("path"));
const versions_cjs_1 = require("./versions.cjs");
const collectionPath = path.join(__dirname, '../collection-test.json');
describe('ng-add', () => {
    it('adds v7 dependencies to package.json', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runner, appTree } = yield setup();
        const tree = yield runner.runSchematic('ng-add', { project: 'test-app' }, appTree);
        const packageJson = JSON.parse(tree.readContent('package.json'));
        expect(packageJson.dependencies).toBeDefined();
        const dependencies = packageJson.dependencies;
        expect(dependencies['@fortawesome/free-solid-svg-icons']).toBe(versions_cjs_1.iconPackVersionMap['7'].iconPackVersion);
        expect(dependencies['@fortawesome/angular-fontawesome']).toBe(versions_cjs_1.angularFontawesomeVersion);
    }));
    it('adds v6 dependencies to package.json', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runner, appTree } = yield setup();
        const tree = yield runner.runSchematic('ng-add', { project: 'test-app', version: '6' }, appTree);
        const packageJson = JSON.parse(tree.readContent('package.json'));
        expect(packageJson.dependencies).toBeDefined();
        const dependencies = packageJson.dependencies;
        expect(dependencies['@fortawesome/free-solid-svg-icons']).toBe(versions_cjs_1.iconPackVersionMap['6'].iconPackVersion);
        expect(dependencies['@fortawesome/angular-fontawesome']).toBe(versions_cjs_1.angularFontawesomeVersion);
    }));
    it('adds v5 dependencies to package.json', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runner, appTree } = yield setup();
        const tree = yield runner.runSchematic('ng-add', { project: 'test-app', version: '5' }, appTree);
        const packageJson = JSON.parse(tree.readContent('package.json'));
        expect(packageJson.dependencies).toBeDefined();
        const dependencies = packageJson.dependencies;
        expect(dependencies['@fortawesome/free-solid-svg-icons']).toBe(versions_cjs_1.iconPackVersionMap['5'].iconPackVersion);
        expect(dependencies['@fortawesome/angular-fontawesome']).toBe(versions_cjs_1.angularFontawesomeVersion);
    }));
    it('adds FontAwesomeModule import to the AppModule when standalone=false', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runner, appTree } = yield setup(false);
        const tree = yield runner.runSchematic('ng-add', { project: 'test-app' }, appTree);
        const contents = tree.readContent('src/app/app-module.ts');
        expect(contents).toContain('import { FontAwesomeModule }');
    }));
    it('does not attempt to add FontAwesomeModule import to the AppModule when standalone=true', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runner, appTree } = yield setup();
        const tree = yield runner.runSchematic('ng-add', { project: 'test-app' }, appTree);
        expect(tree.files).not.toContain('/src/app/app.module.ts');
    }));
    it('installs @fortawesome/free-solid-svg-icons package by default', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runner, appTree } = yield setup();
        const tree = yield runner.runSchematic('ng-add', { project: 'test-app' }, appTree);
        const packageJson = JSON.parse(tree.readContent('package.json'));
        expect(packageJson.dependencies).toBeDefined();
        const dependencies = packageJson.dependencies;
        expect(dependencies['@fortawesome/free-solid-svg-icons']).toBeDefined();
    }));
    it('allows to install several @fortawesome/*-svg-icons packages', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runner, appTree } = yield setup();
        const tree = yield runner.runSchematic('ng-add', {
            project: 'test-app',
            iconPackages: ['free-solid', 'free-brands', 'free-regular'],
        }, appTree);
        const packageJson = JSON.parse(tree.readContent('package.json'));
        expect(packageJson.dependencies).toBeDefined();
        const dependencies = packageJson.dependencies;
        expect(dependencies['@fortawesome/free-solid-svg-icons']).toBeDefined();
        expect(dependencies['@fortawesome/free-brands-svg-icons']).toBeDefined();
        expect(dependencies['@fortawesome/free-regular-svg-icons']).toBeDefined();
    }));
    it('allows to install no icon packages', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runner, appTree } = yield setup();
        const tree = yield runner.runSchematic('ng-add', {
            project: 'test-app',
            iconPackages: [],
        }, appTree);
        const packageJson = JSON.parse(tree.readContent('package.json'));
        expect(packageJson.dependencies).toBeDefined();
        const dependencies = packageJson.dependencies;
        expect(dependencies['@fortawesome/free-solid-svg-icons']).not.toBeDefined();
    }));
});
const setup = (standalone) => __awaiter(void 0, void 0, void 0, function* () {
    const runner = new testing_1.SchematicTestRunner('schematics', collectionPath);
    const appTree = yield runner.runExternalSchematic('@schematics/angular', 'ng-new', {
        name: 'test-app',
        version: '9.0.0-rc.6',
        directory: '.',
        standalone,
    }, schematics_1.Tree.empty());
    return { runner, appTree };
});
//# sourceMappingURL=index.spec.cjs.map